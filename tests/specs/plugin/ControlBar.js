import expect from 'expectations';
import ControlBar from '../../../src/js/plugin/ControlBar.js';

describe('ControlBar', function () {
    it('is exported', function () {
        expect(ControlBar).toBeDefined();
    });
});
