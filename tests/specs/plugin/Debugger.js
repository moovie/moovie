import expect from 'expectations';
import Debugger from '../../../src/js/plugin/Debugger.js';

describe('Debugger', function () {
    it('needs to have tests written', function () {
        expect(Debugger).toBeDefined();
    });
});
