import MediaEvents from '../../../src/js/core/media-events.js';

describe('MediaEvents', function () {
    it('gets exported as a default module', function () {
        expect(MediaEvents).toBeDefined();
    });
});
