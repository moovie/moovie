import expect from 'expectations';
import SRTCue from '../../../../src/js/core/track/SRTCue.js';

describe('SRTCue', function () {
    it('gets exported as a named module', function () {
        expect(SRTCue).toBeDefined();
    });
});
