import HTMLTrackElement from '../../../src/js/track/html-track-element.js';

describe('HTMLTrackElement', function () {
    it('gets exported as a default module', function () {
        expect(HTMLTrackElement).toBeDefined();
    });
});
